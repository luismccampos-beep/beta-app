// apps/frontend/src/types/user.ts
export type { User, UserPreferences, UserAddress, SocialMedia, UserRole, ProfileVisibility } from './auth';

// If you need to extend or add frontend-specific user types, do it here.
// For now, we are re-exporting everything from shared to avoid duplication.
