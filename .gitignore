# Windows reserved device file
nul

# Dependencies
node_modules/

# Next.js
.next/
out/

# Env (secrets — never commit)
.env
.env.local
.env.*.local
.env.prod
.env.prod2
.env.production
.env.staging
.env.development
.env.localdev.bak
!.env.example

# Cloudflare Workers secrets (local dev — never commit)
**/.dev.vars
!**/.dev.vars.example

# Debug / logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# TypeScript
*.tsbuildinfo

# Testing
coverage/
.playwright/
test-results/
playwright-report/

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
__pycache__/
*.py[cod]

# Large data directories (versioned datasets should move to object storage)
Database/
data/
travel-images-data/
!apps/web-tanstack/src/components/data/
src/data/

# Data exports (artefactos temporários — manter em data/ ou storage externo)
export_*.csv
export_*.json
lugares_reais_*.csv
wikivoyage_links.csv
wikivoyage_links.json
wikivoyage_links.html
wikivoyage_links.md
wikivoyage_cache.json

# Analysis artefacts
lighthouse-*.json
ENRICHMENT-SUMMARY.md
GEOCODING-SUMMARY.md
COMMIT_MSG.md

# Prisma (optional local DB dumps)
*.db
*.db-journal
.env*.local

# Backup files
*.bak

# Wikivoyage dumps & extracted JSONL (large; keep locally in data/)
data/*.xml.bz2
data/wikivoyage/out/
data/hotels/hotel-index.json
src/data/travel-mock/bundle-wikivoyage.json
# unsplash-cache.json is committed (~2.6MB)
data/hotels/tbo-hotels.csv

# Internal work notes (may contain secrets)
.summary.md

# Orphaned submodules (local only)
Zoo-Code/

# Storybook
storybook-static/

# Turborepo cache (generated; do not commit)
.turbo/
apps/web-tanstack/.turbo/

# TanStack Router cache (generated; do not commit)
.tanstack/

# MCP configs (local tooling, not part of the app)
.mcp/

# Build outputs (do not commit)
apps/web-tanstack/dist/
deploy/tanstack-poc/dist/
apps/web/.open-next/

# Wrangler (do not commit)
.wrangler/
apps/web-tanstack/.wrangler/
deploy/tanstack-poc/.wrangler/

# Dependencies
apps/web-tanstack/node_modules/
